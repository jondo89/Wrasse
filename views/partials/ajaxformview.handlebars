<h5 class="display-3">Basic Form via Ajax</h5>
<p>Depoly the basic form using a handlebars partial. A hidden id can be injected into the form to group issues by a topic or ID.</p>


<div class="card">
 <div class="card-body">
  <h5>Demo</h5><br>
  <div id="ajaxFormLocations">
    <button href="" class="btn btn-success" onclick="getForm('ajaxFormLocations')">Flag an issue</button>
  </div>
</div>
</div>
 
             

<script type="text/javascript">
  function getForm(location) {
    jQuery.ajax({
      'async': true,
      'global': false,
      'url': '/issues/ajax/'+'wrasseform',
      'success': function (dataSchemaObject) {
        jQuery('#'+location).html(dataSchemaObject)
      }
    });
  }
</script>