
 
<div class="row">
        <div class="col-6" style="max-height: 450px;overflow: auto">
<h5>The routing of the forms.</h5>
<div class="list-group">
 {{#each routing}}
  <h5 class="mb-1">{{@key}}</h5>
  {{#each this}}  
  <a  class="list-group-item list-group-item-action">
    <div class="d-flex w-100 justify-content-between"> 
       <code>{{this.url}}</code>
       <code>{{this.type}}</code>
    </div>
    <p class="mb-1">{{this.description}}</p>
    
    <button onclick="getForm('ajaxFormLocationmodal')" data-toggle="modal" data-target="#exampleModalScrollable" class="btn btn-sm btn-outline-primary">Flag an issue</button>
  </a>
               {{/each}}
        {{/each}}   
</div>
        </div>
        <div class="col-6" style="max-height: 450px;overflow: auto">
                    <h5>The primary routing JSON object deployed directly from the node server.</h5>
                <code>NULL routes are due to server JSON.stringify passing of the seperate function routes. View the index.js of the npm cleaner-wrasse to understand the middleware routing.</code>
             <pre>{{toJSON routing}}</pre>
        </div>
</div>

<p></p>

 